# Ask Metric 项目开发约定

本文件适用于当前仓库。当前任务中的明确要求优先；修改前结合实际代码和相关文档核对，不把历史阶段说明当作当前已实现能力。

## 提交与推送

- Git 提交标题、提交正文及推送结果说明统一使用简体中文，必要的代码标识符、命令和技术名称保留原样。
- 当前任务中用户明确指定其他语言时，按该任务要求执行。

## 问数与后续分析架构

- 当前运行版本仅维护单次可信问数、当前任务澄清和历史结果查看/导出；旧归因原型退出运行链，不保留 LangGraph 分析运行器、固定归因流程或恢复开关。归因请求须明确说明当前不支持，不能降级为普通取值查询。
- 每次新问题独立解析，不继承其他任务的日期、指标、机构或操作，不解析历史指代、不执行跨任务追问；当前未完成任务通过明确的任务与澄清标识补充条件。查询完成后，“那江阴呢？”作为新问题澄清缺失条件。历史记录只用于展示和导出，不作为新查询上下文。
- 后续接入 harness 时复用可信查询、结果证据、权限和公共计算能力；不为尚未接入的方案预建无调用方的框架。
- 变化额、百分比、贡献度、差额核对及单位换算由受控计算基于原始证据执行，模型只选择动作和引用，不自行计算或猜换算系数。贡献公式须明确分母与涨跌符号，不同维度分别核对，不叠加其贡献度；无依据的业务动因不得作为事实输出。
- 旧归因历史结果仅保留必要的只读展示、导出和权限复核；不得恢复执行或作为普通查询上下文。已发布的数据库迁移历史不改写，历史表和数据按既有治理流程处理。

## 项目入口

- `README.md`：系统组成、语义查询链路、配置和部署入口。
- `backend-next/`：Python / FastAPI 后端；依赖与检查配置见 `pyproject.toml`，运行说明见其 `README.md`。
- `frontend-vue/`：Vue 3 / TypeScript 前端；以 `package.json` 中的脚本为准。
- `deploy/package/`：Linux 原生离线构建、安装、升级和回滚工具。
- `docs/`、`backend-next/docs/`：接口、数据湖对接和数据库治理说明；涉及相应功能时再阅读。

## 语义理解与查询边界

- 模型负责理解自然语言意图、日期、机构集合和查询操作；后端负责结构、目录、日期合法性、权限和查询能力校验。
- 保留正式指标与机构的精确匹配和模型调用前的实体占位保护，避免把指标名称内部的“排名、较上月”等词误当成查询指令。
- 实体保护后，排名、趋势等操作只能依据实体之外的请求语义；正式编码和候选名称不得重新泄漏实体内部的操作含义。保留“谁最高、逐月变化”等等价表达，不以缺少固定关键词删除操作；操作依据不成立时中止并澄清，不降级执行普通取值。
- 短别名嵌在目录中更具体指标的未完整名称内时，不得直接锁定为宽口径指标；保留限定信息并澄清候选，避免“个人活期存款余额当日”被“存款余额”别名覆盖。
- 不因未命中中文关键词就删除模型识别出的操作或日期，不用词表擅自补出操作、覆盖排序参数或时间模式。
- 实体之外的原文明示日期独立接受日历合法性校验，不因模型返回合法日期而跳过；非法日期须澄清，不截断数字或自动纠错，保留已确认的非时间条件。
- 不仅凭指标中文名称增删查询操作。需要判断指标类型、预计算属性或展示方式时，优先使用明确的目录属性；先核对现有字段，不假定元数据改造已经完成。
- 模型给出的指标、机构和范围必须接受正式目录与服务端权限校验，不能自由生成可执行机构编号或扩大授权范围。
- 一期新查询不执行两期或两机构现场比较计算（`period_compare`、`entity_compare`），后端统一拦截；正式目录已有的增幅、排名指标仍可取值，历史比较结果保留只读展示与导出。
- 对超出当前查询能力的请求明确返回不支持或需要澄清，不能通过删除操作将其悄悄变成普通取值查询。
- 查询能力按结构化条件统一校验，不用原句“明细、流水”等词拒绝正式指标。外部槽位模型须显式返回操作数组；操作、筛选、维度等关键条件格式错误时中止解析，不丢弃后继续执行。后续 harness 调用同样遵守后端校验。
- 业务查询 SQL 来自 `backend-next/resources/sql/` 和 `config/query-templates.json` 登记模板；业务参数绑定，模型不生成或直接执行 SQL。

## 配置与公共逻辑

- 指标和机构目录对普通用户只读，保留查看、搜索和分页；新增、编辑、启停及删除仅系统管理员可用，前端隐藏修改入口，后端独立校验权限。
- 跨机构数据查询授权与管理员角色分离；按服务端正式机构规则授权：省联社可查全省已启用机构，各行仅本机构，不按账号白名单或机构名称授权，管理员角色不绕过数据范围。

- 修改提示词、语义配置或 SQL 模板前，先核对相应仓库加载方式和运行环境实际使用的文件路径。生产持久配置可能不同于仓库默认文件。
- 配置项必须接入实际调用链。新增或修改可配置行为时，验证配置变化确实改变对应输出，避免保留“读取成功但被忽略”的配置。
- 后端澄清字段和最终澄清提示使用统一配置来源，模板占位符在配置加载时校验。聊天中的指标、机构澄清按结构化缺失字段统一展示简短补充提示，日期等详细提示保留后端文案；首次与历史展示复用同一入口。候选数据保留供输入框目录选择，不在回复正文列举。
- 日期澄清通过简短文案引导用户在原对话补充，不默认代填日期；补充后保留已确认的指标、机构和查询操作。
- 澄清消息只用简短文字说明缺失条件；指标和机构选择集中在聊天输入框，仅当前会话本次提问或补充回答新产生的澄清自动展开所缺目录；进入、刷新或切回历史会话不自动弹出，保留手动点击入口；保留无需前置空格的 `/指标`、`/机构` 快捷搜索。纯日期澄清不弹目录。所选名称插入光标处的正文，与日期文字一次发送，不另列标签或要求中间保存。编辑或删除所选名称后，不提交原选择编号。澄清中的新增目录选择须追加并去重，保留原指标、机构及其他查询条件，不作为替换操作。
- 机器人图标使用中性黑色，历史回复展示持久化的消息时间，不以加载时间代替。输入区与发送按钮对齐，限制长文本输入高度；空白回车不发送、不累积空行，保留 Shift+Enter 换行及中文输入法确认。
- 机器人回复及澄清区域不使用外框和阴影，待补充等普通状态使用中性灰色。回复正文不提供候选点击确认、候选卡片或独立搜索弹窗；选择入口统一在输入框，键盘焦点清晰可见。历史澄清保留只读的简短提示，仅当前任务可通过输入框补充。
- 通用文本规范化、中文数字转换和日期处理优先复用公共实现。稳定算法可以保留在代码中，不把“出现中文”本身视为需要配置化的理由。
- 对经常变化的业务词表、别名和文案优先使用已有目录或配置；不要为个别问法不断新增分散的条件分支。
- 查询响应、计算及导出保留原始数值；结果表格按原单位展示，指标值显示两位小数，户数、排名等整数指标显示整数，不展示单位。前端精度格式化和自然语言单位换算不得回写原始数据。
- 助手回答及结果表格使用聊天区域的完整可用宽度，不因展开历史会话栏设置固定最大宽度而留下大片空白。
- 多指标查询仅返回部分指标时，回复应说明本次机构和时间范围内未查到的指标；不把排名未入选或结果截断视为缺少数据，不为缺失项填零。
- 查询结果按明确请求的日期或时间窗口、机构和指标组合说明缺失数据；连续区间不自行假定每日均应有记录。调试窗口展示模板代入参数后的完整SQL，实际执行仍使用参数绑定，诊断SQL不得回用于执行。
- 排名未满TopN或TopN覆盖全部指定机构时，说明可确认无数据的机构；已满TopN时不得仅凭未返回断定机构无数据。
- 缺数据说明按日期、指标归并，全范围缺失不逐家重复列举；回复正文默认限制长度并提供展开/收起，展开区域限制高度，完整内容可复制，结果表格保留分页，避免有数据或无数据时无限铺开。
- 趋势回复说明实际统计日覆盖，将无记录日期合并为区间，只有一个统计日时明确无法判断变化趋势；不因最后一个日期命中就宣称覆盖完整。少数机构有值、其余多数无值时用“其余N家”归并说明，避免重复长名单。

## 数据库与部署

- 启用 Embedding 时，后端启动即后台初始化指标目录向量；初始化完成前前端提示进度并禁用提问，后端同步拦截问数与测试执行。初始化失败须明确提示并重试，历史结果仍可访问。目录向量采用连续 float32 存储，模型调用协议、目录语义、召回规则和业务数值精度保持独立。

- 演示与回归数据使用正式目录中的指标编码、名称和原始单位，不另建同名模拟指标；生成数值应有合理规模和非整齐尾数，并明确标注为演示数据，测试题须覆盖原值到展示单位的换算。
- 应用元数据使用 `APP_DATABASE_URL`，指标查询使用独立用途的 `QUERY_DATABASE_URL` 和数据库侧只读账号。
- 数据库结构变更遵循 GoldenDB Alembic 链和既有治理流程：先生成离线 SQL，经 DBA 审核及相应授权后再执行；不得借普通验证任务绕过变更开关。
- 验证优先采用无需真实数据库或模型的本地方式。确需联调时核对目标环境，遵守已有测试与写入开关，不能把联调结果描述为生产验证。
- 模型地址、密钥和连接凭据使用既有环境配置与加密机制，不写入源码、规范文件、日志或查询响应。
- 数字农商 SSO 模式由平台验证身份并映射机构，用户不维护问数密码；首次认证自动建立审计影子身份。刷新恢复本地有效会话，过期或退出提示从数字农商重新进入，不落到密码登录页。
- 浏览器会话有效期内刷新可恢复登录，恢复不延长原有效期；访问令牌不落 Web Storage，恢复 Cookie 使用 HttpOnly、SameSite 和生产 Secure，并校验请求来源。退出、过期及账号或会话撤销后不得恢复，业务接口继续校验 Bearer 与服务端权限。
- 上下文解析异常须记录失败阶段、重试次数、字段位置与校验类型，并关联请求及任务；任务调试信息保留脱敏诊断摘要，不记录完整模型响应或异常原始输入。
- 涉及部署时保持原生离线部署能力；前端发布使用同源 `/api`，升级应遵循持久配置保护规则。

## 验证与交付

- 关键运行链保留简洁中文注释，说明职责、数据流、权限/事务/并发边界及非直观语法的意图；面向具备基础 Python 的维护者，复杂表达式优先拆为有意义的步骤。详细语法示例与调用链维护在 [关键代码阅读指南](docs/关键代码阅读指南.md)，不逐行复述代码。

按改动范围执行必要检查，不因文档修改运行真实服务或数据库测试。本源码交付不包含维护仓库的自动化测试集；下列测试命令需在持有对应测试集的维护环境执行。

- 后端逻辑修改：在 `backend-next/` 执行 `python -m pytest -q`、`python -m unittest discover -s tests -v` 和 `python -m ruff check src scripts tests`。
- 语义逻辑修复：覆盖原始问法、等价表达、指标名称保护、无效输入及不支持的查询；固定模型响应测试与真实模型准确率测试分别报告。
- 前端类型或逻辑修改：在 `frontend-vue/` 执行 `npm run typecheck`；涉及构建或发布时执行 `npm run build`。
- 提交交付前检查相关差异和 `git diff --check`；说明实际执行的检查与限制。
- 对外行为、配置使用方式或部署操作变化时同步相关文档，不将已删除逻辑继续写成当前能力。

## 本项目要求的维护

- 用户明确新增或修订长期项目要求时，去重更新本文件，并在交付时说明变化。
- 不将尚未实施的建议写成已完成事实，不把“本次先只修某一部分”等临时范围限制保存为永久禁令。
- 当前开发进度以代码、任务记录和专项文档为准；本文件保存长期约束，不保存逐次会话流水。
