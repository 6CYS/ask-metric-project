# AGENTS.md

本文件只约定在本仓库进行开发、验证和交付的工程规范。产品能力、业务口径、运行时提示词、页面交互和阶段性任务范围不在此定义；相应内容写入专项设计、配置、实现及测试。

## 工作方式

- 开始前阅读当前任务、相关代码和直接关联的文档，核对实际调用链，不把旧文档或既有实现自动视为正确需求。
- 按当前明确任务完成必要修改；复用现有模块和公共逻辑，保持改动聚焦，不夹带无关重构。确需调整既有设计时，同步说明原因及影响。
- 保留用户已有改动。未经明确要求，不执行提交、推送、强制重置、删除数据或其他破坏性操作。
- 清晰且已获授权的任务直接执行；涉及未获授权的外部写入、不可逆操作或实质不同的产品方向时再确认，不反复确认已经明确的要求。
- 依赖变更仅限当前任务所需，说明用途和版本影响，维护对应锁文件，不顺手升级无关依赖。

## 仓库导航

- `README.md`：项目组成、配置与运行入口。
- `backend-next/`：Python / FastAPI；检查与依赖以 `pyproject.toml` 和该目录说明为准。
- `agent-service/`：Node / TypeScript；使用其 `package.json` 中实际存在的脚本。
- `frontend-vue/`：Vue / TypeScript；使用其 `package.json` 中实际存在的脚本。
- `deploy/package/`：构建、部署、升级与回退工具。
- `docs/`、`backend-next/docs/`：按任务阅读直接相关的设计与操作说明，不将全部文档无差别加载。

## 编码与接口

- 沿用项目命名、目录、格式与类型约定；优先清晰表达、显式边界和可测试实现，避免重复封装和无调用方的抽象。
- 修改接口或持久化结构时，同步检查调用方、兼容性、错误处理及迁移需求，不用静默兜底掩盖不兼容。
- 对外部输入、工具结果及跨服务响应做适当校验；失败应返回可识别错误，不吞掉异常或把未知状态当成功。
- 关键边界和非直观实现使用简洁中文注释，说明职责、数据流及并发/事务含义，不逐行复述代码。
- 配置与提示词修改前核对加载入口和实际生效路径。新增配置应接入调用链并有验证；稳定算法不为配置化而配置化。
- 业务行为调整同时更新相应实现、配置和测试，不以开发说明文件代替运行时校验，不把开发规范注入业务模型提示词。

## 安全与数据

- 不将凭据、令牌、连接秘密或未脱敏敏感内容写入代码、文档、日志或交付物；使用既有安全配置方式。
- 保持服务端鉴权、资源归属与数据访问边界；前端校验、模型判断和提示词不能替代服务端控制。
- 验证优先使用本地桩、只读操作或专用可丢弃测试环境。未经授权，不连接生产环境执行测试或修改生产数据。
- 数据结构变更提供增量迁移、兼容与回退说明，遵守目标环境审批流程；不改写已发布迁移历史。
- 日志包含必要关联信息和脱敏诊断，区分可公开错误与内部细节，不输出完整敏感输入或响应。

## 验证与交付

- 先运行能覆盖改动的最小检查，再按风险扩大回归；只改文档时不因此启动真实模型、业务服务或数据库测试。
- 修复缺陷时补充能暴露问题的回归测试。断言以本次确认的行为和数据契约为准，不机械维护旧规则，也不删除有效测试来掩盖失败。
- 测试数据可使用明确隔离的合成数据、桩或授权样本；不得混入生产事实，也不得将其结果冒充真实环境验证。
- 检查命令以现有脚本和配置为准。依赖、测试集或环境缺失时如实报告；必要时为改动补建测试，不宣称不存在的检查已通过。
- 前端交互变更在条件允许时验证实际操作、键盘输入、异常状态及刷新恢复；不因本文件固定具体视觉样式。
- 交付前检查相关差异；在 Git 工作树中执行 `git diff --check`。同步受影响的接口、配置和部署说明，清理失效引用。
- 交付说明列出修改内容、实际执行的检查及结果、未验证项和必要的兼容/回退注意事项，区分设计完成与代码实现完成。
- 默认使用简体中文沟通；用户要求提交代码时，提交说明使用简体中文，代码标识符和技术名称保留原样。当前任务另有语言要求时遵循该要求。

## 本文件维护

- 这里只新增或修订长期有效的工程规范，并去重保持简短。
- 业务需求变更写入相关需求或设计、配置、实现和测试，不因为用户提出长期业务要求就追加到本文件。
- 不记录逐次会话流水，不将临时交付范围固化为永久禁令，不将尚未实现的建议写成已实现能力。
