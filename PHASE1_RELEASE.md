# 一期投产源码版本

本分支用于留存一期问数系统部署源码，与客户 `wangdh` 分支的 pi agent harness 集成版本独立维护。

- 客户仓库：`6CYS/ask-metric-project`。
- 留存分支：`codex/phase1-production-20260916`。
- 一期基线：`876c66d0565fecf286bedad483253336c577948f`（精简会话标题栏并移除轮次标签）。
- 范围：保留 Vue 前端、FastAPI 可信问数、当前任务澄清、历史结果和导出、目录和权限校验，以及结构化基础查询接口。聊天直接调用后端问数链路。
- 本分支不包含后续 pi agent harness、`agent-service` 和相应代理、服务及前端切换；不要直接合并 `wangdh` 的后续集成提交来更新一期。

## 本次补充修复

问数日志采用服务端分页，默认每页 10 条；列表、总数及详情只允许当前账号查看本人记录，管理员同样遵守。完整调试信息点击后才读取；机构完整信息悬停或展开时按需读取。新记录保存完整匹配名称；旧记录从查询计划或同次执行结果证据恢复，缺失证据时明确提示不能恢复。

这些修复使用既有表和 JSON 字段，不新增数据库结构迁移，不修改历史业务数据。

## 部署与升级

- 部署入口：[原生离线部署说明](deploy/package/README.md)，按目标 CPU 架构及 Python 版本构建。
- 后端配置：[后端运行与配置说明](backend-next/README.md)。真实数据库地址、模型密钥及持久配置使用目标环境既有配置，不从开发机复制。
- 前端配置：[前端构建说明](frontend-vue/README.md)。生产使用同源 `/api`；不要将本机 `.env.local` 或 `127.0.0.1:18010` 编入前端。
- 升级须遵守既有备份、持久配置保护、迁移审核、验收和回滚流程。本分支的建立不代表已执行客户环境部署或生产验收。
- 本机现场辅助工具、测试数据、报告和临时包不随此次源码留存新增交付；需要这些材料时应独立确认交付范围。

部署时记录本分支实际提交编号与构建包校验值，避免仅以会移动的分支名称识别已投产版本。
