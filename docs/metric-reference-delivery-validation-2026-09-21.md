# 完整指标名与多步结果交付验证

本次针对已定位的两个缺陷：短别名命中覆盖用户明确的完整指标名；宿主将取数和排查覆盖列表全部拼入回答。业务工具仍由 pi 选择，业务知识不承担路由职责。

## 实现边界

- 结构化取数复用 `MetricMatcher` 完整名称匹配，通过宿主绑定的用户原文校验名称冲突，创建任务前返回可纠正的 422；不静默换编码。完整编码、合法别名、多个指标分别取数、不完整追问保持原契约。
- pi 通过 `answer_present` 选择当前回合的证据与顺序；引用、未解决失败和被替代回执由宿主校验，数值、单位和截断信息原样保留。pi 已选择结束却未提供引用时，下一模型步骤约束为交付格式；校验失败仍能回到普通工具循环。
- 最终快照将未选择回执标记为不参与业务展示，保留完整执行日志。实时和重开会话共用投影。既有历史正文不迁移。

## 实际检查

- Backend：`pytest tests/test_structured_metric_reference.py tests/test_catalog_code_search.py tests/test_calculation_contract.py -q`，31 项通过；涉及 Python 文件 Ruff 通过。
- Agent：全量 23 个文件、146 项测试通过；类型检查与构建通过。新增链路用例覆盖冲突反馈、重新检索、改查正确指标、排除无关覆盖目录、强制交付格式及原生会话重开。
- Frontend：现有 17 项测试、类型检查与构建通过。构建仍提示部分产物超过 500 kB，本轮未进行拆包。
- `git diff --check` 通过。
- 真实配置模型 `Qwen/Qwen3.6-35B-A3B` + 合成后端：取数、计算及最终两项原值和差值交付通过；最新探针耗时约 8.1 秒，工具校验失败 0 次，未解决失败为 0。

真实模型探针初次失败揭示旧桩目录误带数值和事实引用、缺少编码回查以及只模拟结构化入口，已改为正式目录字段并覆盖两条合法取数入口；另一次失败来自探针错误限制变量命名，已改为按绑定事实校验。此前也实际观察到模型跳过交付工具，才补齐最终交付格式约束。探针保留途中校验失败计数，验收要求最终目标完整且无未解决失败。

可复现探针：在 `agent-service` 运行 `node --env-file=.env --import tsx scripts/check-skill-model.ts`，使用配置模型但仅接入合成后端，不访问业务数据库。该探针不是后端语义解析或真实业务数据验收。

## 运行与限制

2026-09-21 13:30:12（北京时间）已重新启动当前 Harness 后端、Agent 和前端，健康、就绪及页面入口均返回 200。启动进程与源码/构建摘要记录在 `.runtime/restart-latest/manifest.json`。访问 `http://127.0.0.1:5173`，避免 `localhost` 命中另一个项目的 IPv6 服务。

本轮没有重放真实业务数据库查询或完成浏览器点击验收；没有证明所有自然语言、多轮指代和澄清场景稳定，也没有修复此前发现的全部澄清上下文问题。需要同步更新 Agent 和前端；无数据库迁移，回退前应结束活动回合。工作区原有修改与暂存状态保留，未提交或推送。
